# Setup Guide

1. Clone the repository.
2. Run `npm install`.
3. Set up your `.env` file with `GROQ_API_KEY`.
4. Ensure you have python installed and run `pip install flask flask-cors edge-tts`.
5. Run `npm start` to start both frontend and backend.
